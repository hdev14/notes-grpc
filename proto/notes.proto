syntax = "proto3";

service NoteService {
  rpc List (Void) returns (NoteListRequest);
  rpc Find (NoteFindRequest) returns (NoteFindResponse);
}

message Void {}

message Note {
  int32 id = 1;
  string title = 2;
  string description = 3;
}
